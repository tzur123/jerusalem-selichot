Open station video pages fully, add walking icon and richer nav map pin popup

- Remove QR-scan/arrival gating on /station/[slug] and its video API - the
  video is now always accessible to any visitor, tracked normally
- Add a walking-person icon next to the estimated walking time in the
  navigation route progress bar
- Enlarge the station pin popup on the live navigation map: adds a station
  thumbnail image and a direct watch-video link alongside navigate
- Tour map station sheet always offers watch-video now, not just when
  previously unlocked
